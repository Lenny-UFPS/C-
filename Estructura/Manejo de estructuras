// Crear estructuras en C++  ||  Manejo de estructuras

//Vaciar el buffer y permitir digitar valores [ fflush(stdin); ]
//quitar notación científica con cout.precision(cantidad de decimales) e imprimir con cout<<fixed<<"";

struct nombreEstructura{
  tipoDato  nombreVariable  // Variables de la estructura a trabajar
}nombreVariables  [OPCIONAL];

// Anidar estructuras
struct otraEstructura{
  struct nombreEstructura nombreVariable;
}nombreVariables  [OPCIONAL];

int main(){
  // declarar variables de la estrutura en el main
  nombreEstructura nombreVariable;
  nombreVariable. [Parámetro de la estructura];
  return 0;
}
